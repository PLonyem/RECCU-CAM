import { prisma } from "@/lib/prisma";
import { Card } from "@/components/ui/Card";
import { createMediaMetadata } from "@/app/admin/(dashboard)/operations/actions";

export default async function MediaLibraryPage() {
  const media = await prisma.mediaAsset.findMany({ orderBy: { createdAt: "desc" } });
  return <div className="space-y-7"><header><p className="text-xs font-semibold uppercase tracking-[0.14em] text-gold-strong">Reusable assets</p><h1 className="mt-2 font-display text-3xl font-bold text-institutional">Media Library</h1><p className="mt-2 text-slate-600">The deployment has separate storage-backed upload flows. This library records metadata without pretending a file was permanently uploaded.</p></header><Card className="p-6"><h2 className="font-semibold text-institutional">Register media metadata</h2><form action={createMediaMetadata} className="mt-5 grid gap-4 sm:grid-cols-2"><input required name="title" placeholder="Asset title" className="rounded-lg border border-slate-300 px-3 py-2" /><input required name="fileName" placeholder="File name" className="rounded-lg border border-slate-300 px-3 py-2" /><input required name="fileType" placeholder="MIME type, e.g. image/webp" className="rounded-lg border border-slate-300 px-3 py-2" /><input required name="altText" placeholder="Alternative text" className="rounded-lg border border-slate-300 px-3 py-2" /><textarea name="caption" placeholder="Caption (optional)" className="rounded-lg border border-slate-300 px-3 py-2 sm:col-span-2" /><button className="rounded-lg bg-institutional px-4 py-2 font-semibold text-white sm:col-span-2">Save metadata</button></form></Card><div className="grid gap-4 md:grid-cols-2">{media.map((asset) => <Card key={asset.id} className="p-5"><p className="font-semibold text-slate-900">{asset.title}</p><p className="mt-1 text-sm text-slate-500">{asset.fileName} · {asset.fileType}</p><p className="mt-3 text-xs font-semibold uppercase text-amber-700">{asset.storageState}</p></Card>)}</div></div>;
}
